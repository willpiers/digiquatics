<%= bootstrap_form_for(@pool, style: :horizontal, left: "col-sm-4", right: "col-sm-8") do |f| %>
  <% if @pool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pool.errors.count, "error") %>
        prohibited this pool from being saved:</h2>
      <ul>
      <% @pool.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.text_field :name %>
    <%= f.number_field :location_id %>

  <div class="actions">
    <%= f.form_group do %>
      <%= f.submit %>
    <% end %>
  </div>
<% end %>
