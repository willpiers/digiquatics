<!-- Defaults to records location for edit -->
<div ng-init="location_id = <%= @chemical_record.location.id %>"></div>
<select ng-model='location_id'
        class='form-control'
        ng-options='location.id as location.name for location in locations'>
</select>
<%= f.hidden_field :location_id, value: "{{location_id}}" %>
