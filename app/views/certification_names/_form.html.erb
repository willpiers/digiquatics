<%= form_for(@certification_name) do |f| %>
  <% if @certification_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@certification_name.errors.count, "error") %> prohibited this certification_name from being saved:</h2>

      <ul>
      <% @certification_name.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :account_id %><br>
    <%= f.number_field :account_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
