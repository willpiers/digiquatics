  <%= bootstrap_form_for(@help_desk) do |f| %>
    <%= f.text_field :name, label: 'Description' %>
    <%= f.select :urgency, %w[Low Medium High], label: 'Urgency' %>
    <%= f.file_field :help_desk_attachment %>

    <%= f.primary "Submit"%>
  <% end %>

