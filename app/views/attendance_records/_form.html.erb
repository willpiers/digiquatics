<%= bootstrap_form_for(@attendance_record,
  style: :horizontal, left: 'col-sm-4', right: 'col-sm-8') do |f| %>
  <% if @attendance_record.errors.any? %>
    <div id='error_explanation'>
      <h2><%= pluralize(@attendance_record.errors.count, 'error') %>
        prohibited this attendance_record from being saved:</h2>
      <ul>
        <% @attendance_record.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%= f.text_field :name %>
  <%= f.form_group do %>
    <%= f.primary %>
  <% end %>
<% end %>
